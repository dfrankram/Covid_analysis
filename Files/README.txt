Para ejecutar app.py

En una terminal (por ejemplo de Visual Studio Code) ejecutar los siguientes comandos:

1) pip install pipenv (si no está instalado)
2) pipenv install streamlit pandas requests datetime sodapy scipy numpy seaborn matplotlib plotly

Una vez creado el entorno virtual, escribir el siguiente comando para ejecutar la app:

3) pipenv run streamlit run app.py

Se abrirá una presentación en una pestaña de su navegador

Para finalizar el proceso, volver al terminal y presionar CTRL+C 